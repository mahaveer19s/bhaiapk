version: '3.7'

services:
  pocketbase:
    build: .
    container_name: bhai_pocketbase
    ports:
      - "8080:8080"
    volumes:
      - ./pb_data:/pb/pb_data
      - ./pb_public:/pb/pb_public
      - ./pb_migrations:/pb/pb_migrations
    restart: unless-stopped
